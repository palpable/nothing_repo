<h2>New post</h2>

<%= error_messages_for :cb_post %>

<%= render :partial => 'forum_form',
           :locals => { :url => cb_forum_cb_topic_cb_post_path(@cb_forum, @topic),
                        :method => :post } %>

<%= link_to 'Back', cb_forum_cb_topic_path(@cb_forum, @topic) %>
