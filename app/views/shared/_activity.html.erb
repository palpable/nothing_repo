<%# The activity shouldn't be nil, but we're wearing a belt and suspenders. %>
<%- unless activity.item.nil? -%>
<%- if current_person?(activity.person) -%>

<tr>
<td>
  	<ul class="newsInfo">
    <li><%= feed_icon activity %></li></ul></td><td>
      <ul class="newsInfo">
    <li><%- recent ||= false -%></li>
    <li><%= feed_message(activity, recent) %></li>
    </ul>
  </td>
  <td>
    <%= time_ago_in_words(activity.created_at).capitalize %> ago
      <%= link_to(image_tag("icons/comment_delete.png",
                             :class => 'actioner'), 
                   activity_path(activity), 
                   :method => :delete, 
                   :confirm => %(Are you sure you want to delete this 
                      activity from your profile?),
                   :title => "Delete this activity") %>
  </td>
</tr>
<%- end -%>
<% end %>
